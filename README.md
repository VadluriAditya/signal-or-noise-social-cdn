# signal-or-noise-social-cdn

Temporary public file host used by the Signal or Noise automation pipeline to hand rendered video clips to Instagram's Reels API (which requires a publicly fetchable URL before it will publish). Files here are transient — pushed right before a post, deleted right after. Nothing else lives in this repo.
